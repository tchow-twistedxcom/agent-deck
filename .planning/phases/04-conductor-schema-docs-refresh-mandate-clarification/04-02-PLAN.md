---
phase: 04-conductor-schema-docs-refresh-mandate-clarification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
  - CLAUDE.md
  - "~/.claude/plugins/cache/agent-deck/agent-deck/<hash>/skills/agent-deck/SKILL.md"
  - "~/.agent-deck/skills/pool/agent-deck/SKILL.md"
  - ".planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md"
autonomous: true
requirements: [CFG-09, CFG-10]
requirements_addressed: [CFG-09, CFG-10]

must_haves:
  truths:
    - "README.md's existing `### Per-group Claude config` subsection (added in Phase 3 plan 03-02) gains a sibling `[conductors.<name>.claude]` example and a one-line precedence note."
    - "README.md subsection cross-links to issue #602."
    - "agent-deck SKILL.md at the canonical plugin-cache path is updated with a one-paragraph block documenting the `[conductors.<name>.claude]` schema, precedence chain, and the pool-vs-canonical distinction."
    - "agent-deck SKILL.md at the pool path (if present on this host) is updated with the same paragraph."
    - "A repo-visible audit artifact `.planning/phases/04-.../SKILL_MD_DIFF.md` exists capturing the diff applied to each SKILL.md (or documenting absence of each external path) — guarantees CFG-09 closure is auditable even when external paths are unavailable."
    - "Repo-root CLAUDE.md `## General rules` block gains a new `**No `--no-verify`**` bullet (establishing the base rule — it does NOT currently exist in the file) immediately followed by a `### --no-verify scope clarification` sub-section listing metadata-only paths (`.planning/**`, `docs/**`, `*.md` outside source dirs, `CHANGELOG.md` during milestone-prep) and includes a negative example (mixed `.planning/` + `internal/session/*.go` = source-modifying, hooks required)."
    - "Every Phase 4 commit in this plan is signed `Committed by Ashesh Goplani`, carries no `@alec-pinson` attribution, carries no Claude attribution, and at least the README commit body references issue #602."
  artifacts:
    - path: "README.md"
      provides: "Extended Per-group Claude config subsection with conductor example + precedence note + #602 cross-link"
      contains: "[conductors.<name>.claude]"
    - path: "CLAUDE.md"
      provides: "NEW `--no-verify` bullet in ## General rules + `--no-verify scope clarification` sub-section with metadata-paths list + negative example"
      contains: "--no-verify scope clarification"
    - path: "~/.claude/plugins/cache/agent-deck/agent-deck/<hash>/skills/agent-deck/SKILL.md"
      provides: "Canonical-plugin-cache skill documents conductor-block schema"
      contains: "[conductors.<name>.claude]"
    - path: ".planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md"
      provides: "Mandatory repo-visible audit artifact for external SKILL.md updates. Contains the exact diff applied to canonical + pool SKILL.md, OR explicit absence documentation per path."
      contains: "canonical\\|pool\\|SKILL.md"
  key_links:
    - from: "README.md Per-group Claude config subsection"
      to: "issue #602"
      via: "Markdown link to https://github.com/asheshgoplani/agent-deck/issues/602"
      pattern: "issues/602"
    - from: "CLAUDE.md General rules"
      to: "metadata-only paths list"
      via: "bulleted list of paths exempt from the --no-verify ban"
      pattern: "metadata-only"
    - from: "CLAUDE.md new --no-verify bullet"
      to: "--no-verify scope clarification sub-section"
      via: "rule-then-clarification layout — bullet establishes the ban, sub-section scopes it"
      pattern: "No `--no-verify`"
---

<objective>
Refresh the two documentation surfaces that weren't covered by Phase 3's CFG-06 (agent-deck SKILL.md at canonical + pool paths; README's conductor-block extension), and establish + clarify the repo-root `CLAUDE.md` `--no-verify` mandate so metadata-only commits don't pay hook latency for zero verification value. Covers CFG-09 (docs refresh) and CFG-10 (mandate + clarification).

Purpose:
- **Discoverability.** The agent-deck skill at `~/.claude/plugins/cache/agent-deck/agent-deck/<hash>/skills/agent-deck/SKILL.md` is what OTHER Claude sessions read when they load the agent-deck skill. If the new `[conductors.<name>.claude]` block isn't documented there, users of agent-deck from separate Claude instances won't discover it. Same for the pool copy at `~/.agent-deck/skills/pool/agent-deck/SKILL.md` if present. A mandatory repo-visible `SKILL_MD_DIFF.md` artifact makes the external-file change auditable regardless of whether the plugin cache is present on the execution host.
- **User recall.** README.md is the canonical on-repo docs. Phase 3's CFG-06 added the `### Per-group Claude config` subsection but pre-dates Phase 4's conductor block — extending it is strictly additive.
- **Mandate UX — establish THEN clarify.** The repo-root `CLAUDE.md` `## General rules` block CURRENTLY contains no `--no-verify` line at all (verified: `grep -n "no-verify" CLAUDE.md` returns zero matches). Phase 4 ships the base ban as a new bullet AND the metadata-only scope clarification in the same edit. Source-modifying commits continue to require hooks; metadata-only commits (`.planning/`, docs, CHANGELOG.md) are exempt.

Output:
- README.md gains one extended block under its existing Per-group Claude config subsection.
- CLAUDE.md gains a NEW `**No `--no-verify`**` bullet in `## General rules` AND a new `### --no-verify scope clarification` sub-section beneath that block.
- Canonical SKILL.md (and pool SKILL.md if present) gain one paragraph each.
- MANDATORY `.planning/phases/04-.../SKILL_MD_DIFF.md` records the diff(s) applied or documents path absence.
- Commits:
  1. README extension — SOURCE-touching (hooks enabled).
  2. SKILL.md updates — external files; repo commit for the audit artifact (`SKILL_MD_DIFF.md`) with hooks enabled.
  3. CLAUDE.md mandate + clarification — SOURCE-touching (repo-root, ships with hooks).

All three commits sign `Committed by Ashesh Goplani`, reference issue #602 in at least the README body, and omit @alec-pinson + Claude attribution.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.claude/plugins/cache/gsd@ad3bab3fea6f/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.claude/plugins/cache/gsd@ad3bab3fea6f/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md

<!-- Plan 04-01's SUMMARY — critical for schema shape clarification -->
@.planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-01-SUMMARY.md

<!-- Phase 3 plan 03-02 SUMMARY — contains the existing README subsection this plan extends -->
@.planning/phases/03-visual-harness-documentation-attribution-commit/03-02-SUMMARY.md

<!-- Source docs this plan edits -->
@README.md
@CLAUDE.md

<interfaces>
<!-- The existing README subsection to extend. The exact anchor is TBD by the executor via grep. -->

From Phase 3 plan 03-02 (README.md ### Per-group Claude config subsection):
- Located under a Features or Configuration section
- Contains a `[groups."conductor".claude]` TOML example
- Cross-links to scripts/verify-per-group-claude-config.sh
- The executor MUST find the exact anchor with:
  ```bash
  grep -n "Per-group Claude config" README.md
  ```

From the CURRENT repo-root CLAUDE.md ## General rules block (VERIFIED via `grep -n` at planning time):
- Existing bullets: `Never \`rm\``, `Never commit with Claude attribution`, `Never \`git push\`...`, `TDD always`, `Simplicity first`.
- **There is NO existing `--no-verify` bullet.** `grep -n "no-verify" CLAUDE.md` returns zero matches.
- Plan 04-02 Task 3 establishes the `--no-verify` bullet AND the clarification sub-section in a single commit (rule-then-scope layout).

From Plan 04-01 output (MUST be incorporated verbatim into docs):
- Schema shape is **nested**: `[conductors.<name>.claude]` with `config_dir` and `env_file` keys.
- NOT the flat form `[conductors.<name>]` that the original spec example showed (REQ-8 / CFG-08).
- Precedence chain: env > conductor > group > profile > global > default.
- Conductor name derivation: Instance.Title minus `conductor-` prefix.
- Backward compat: group-only config still resolves correctly (PR #578 contract preserved).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend README.md "Per-group Claude config" subsection with [conductors.<name>.claude] example + precedence note + #602 link</name>
  <files>README.md</files>
  <read_first>
    - README.md (MANDATORY — grep for "Per-group Claude config" first to locate the exact anchor; then read 30 lines before + 30 after to understand the existing subsection's tone, heading level, and code-fence style)
    - .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-01-SUMMARY.md (MANDATORY — the schema shape section must quote the nested `[conductors.<name>.claude]` form, NOT the flat form)
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-02-SUMMARY.md (confirm what CFG-06 shipped in README)
    - docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md (REQ-9 / CFG-09 lines 153-160)
    - .planning/REQUIREMENTS.md (CFG-09 lines 66-68)
  </read_first>
  <action>
**Step 1a — Locate the anchor.** Run:
```bash
grep -n "Per-group Claude config" README.md
```
This returns the heading line. Read the 40 lines starting from that line to see the existing content.

**Step 1b — Append an extension immediately after the existing subsection's content** (before the next heading at the same or higher level). The extension:

```markdown
#### Per-conductor Claude config (v1.5.4)

Conductors are first-class agent-deck entities (see `agent-deck conductor setup`). Each conductor can carry its own Claude `config_dir` and `env_file` via a top-level `[conductors.<name>.claude]` block:

```toml
[conductors.gsd-v154.claude]
config_dir = "~/.claude-work"
env_file = "~/git/work/.envrc"
```

The conductor name is the string you passed to `agent-deck conductor setup <name>` — it's the same name that appears in session titles (`conductor-<name>`).

**Precedence chain** (most-specific → least-specific):

1. `CLAUDE_CONFIG_DIR` env var
2. `[conductors.<name>.claude]` (when the session is a conductor session, i.e. Title starts with `conductor-`)
3. `[groups."<group>".claude]` (PR #578)
4. `[profiles.<profile>.claude]`
5. `[claude]` (global)
6. `~/.claude` (default)

This means a single `[conductors.gsd-v154.claude]` line replaces the need to duplicate the config into `[groups."conductor".claude]` — the conductor block scopes to exactly that conductor, not to every conductor that shares the `conductor` group.

Backward compat: sessions in the `conductor` group with NO matching `[conductors.<name>.claude]` block continue to resolve via `[groups."conductor".claude]` as they did in v1.5.4 Phase 1–3.

Closes [issue #602](https://github.com/asheshgoplani/agent-deck/issues/602).
```

**Notes on tone and level:**
- The existing `### Per-group Claude config` is an `###` heading (level 3). The new `#### Per-conductor Claude config (v1.5.4)` is `####` (level 4) so it nests semantically. If the existing is `##` (level 2), bump this to `###`. Match the file's convention; grep to verify.
- Preserve the code-fence style (triple-backtick with `toml` language tag).
- Do not wrap in HTML.

**Step 1c — Verify the markdown renders** (optional but recommended):
```bash
which pandoc >/dev/null 2>&1 && pandoc README.md -t html -o /tmp/readme.html && grep -A5 "Per-conductor Claude config" /tmp/readme.html
```
This step is advisory only; if pandoc isn't installed, skip.

**Step 1d — Commit.** This is a SOURCE-touching commit (README.md is source, not metadata) — hooks run. Do NOT use `--no-verify`.

```
docs(04): document [conductors.<name>.claude] schema in README (CFG-09)

Extends the existing "Per-group Claude config" subsection added in
Phase 3 (CFG-06) with a sibling "Per-conductor Claude config" block
documenting the new [conductors.<name>.claude] schema, the updated
precedence chain (env > conductor > group > profile > global > default),
and backward compat with PR #578. Cross-links to issue #602.

Refs: https://github.com/asheshgoplani/agent-deck/issues/602

Committed by Ashesh Goplani
```
  </action>
  <verify>
    <automated>
      grep -q "\\[conductors.<name>.claude\\]\\|\\[conductors\\." README.md && grep -q "issues/602\\|issue #602" README.md && grep -q "Per-conductor Claude config" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Per-conductor Claude config" README.md` returns at least 1
    - `grep -q "\\[conductors\\." README.md` (TOML example present)
    - `grep -q "conductors.gsd-v154.claude\\|conductors\\..*\\.claude" README.md` (nested form used, not bare)
    - `grep -q "issues/602\\|issue #602" README.md` (cross-link to #602)
    - `grep -q "Precedence chain\\|precedence" README.md` (precedence note present)
    - `grep -q "Backward compat\\|backward compat" README.md` (backward-compat note present)
    - `grep -B2 -A10 "Per-conductor Claude config" README.md | grep -q "\\[groups"` — the existing group subsection is NOT clobbered (still present nearby)
    - Git log shows commit with subject `^docs\(04\): document \[conductors.<name>.claude\] schema in README \(CFG-09\)$`
    - Commit body contains `https://github.com/asheshgoplani/agent-deck/issues/602` and `Committed by Ashesh Goplani`
    - Commit body does NOT contain `@alec-pinson` or `Co-Authored-By: Claude`
    - Hooks RAN on this commit (verify the commit was NOT created with `--no-verify`): `git log -1 --format=%B` does not contain a "skip hooks" notice; the repo's `.git/hooks/pre-commit` was executed — this is an operational acceptance criterion, not a grep.
  </acceptance_criteria>
  <done>
    README.md's Per-group Claude config subsection is extended with a conductor-block section documenting the nested `[conductors.<name>.claude]` schema, the six-step precedence chain, the backward-compat guarantee, and a link to issue #602. Commit signed "Committed by Ashesh Goplani" without external attributions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update agent-deck SKILL.md at canonical plugin-cache path (and pool path if present) + MANDATORY SKILL_MD_DIFF.md audit artifact</name>
  <files>~/.claude/plugins/cache/agent-deck/agent-deck/&lt;hash&gt;/skills/agent-deck/SKILL.md, ~/.agent-deck/skills/pool/agent-deck/SKILL.md, .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md</files>
  <read_first>
    - The canonical SKILL.md (locate via `ls -d ~/.claude/plugins/cache/agent-deck/agent-deck/*/skills/agent-deck/` — there should be exactly one hash directory; if multiple, pick the most recent)
    - The pool SKILL.md at `~/.agent-deck/skills/pool/agent-deck/SKILL.md` (if the file does NOT exist, skip the pool-update step — confirm absence, document, move on)
    - .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-01-SUMMARY.md (schema shape)
    - docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md (REQ-9 / CFG-09 lines 153-160)
    - .planning/REQUIREMENTS.md (CFG-09 lines 66-68)
  </read_first>
  <action>
**Step 2a — Find the canonical SKILL.md path.**
```bash
ls -d ~/.claude/plugins/cache/agent-deck/agent-deck/*/skills/agent-deck/ 2>/dev/null
```
This prints one or more hash-directories. If multiple, pick the most recently-modified one:
```bash
ls -dt ~/.claude/plugins/cache/agent-deck/agent-deck/*/skills/agent-deck/ 2>/dev/null | head -1
```
Capture the full path into `$CANONICAL_SKILL_DIR`. If zero directories (plugin cache absent on this host), document that in the SKILL_MD_DIFF.md artifact (Step 2e) and skip the actual file edit — the audit artifact is still mandatory.

**Step 2b — Read the canonical SKILL.md** (`$CANONICAL_SKILL_DIR/SKILL.md`) and identify an appropriate insertion point. The skill likely has a "Configuration" or "TOML config" section. If no such section exists, append to the end before any trailing metadata block.

**Step 2c — Insert the following paragraph** (adapt tone to match the existing SKILL.md — it's likely terser than README):

```markdown
### Per-conductor Claude config (v1.5.4+)

Conductors can carry their own Claude `config_dir` and `env_file` via:

```toml
[conductors.<name>.claude]
config_dir = "~/.claude-work"
env_file = "~/git/work/.envrc"
```

Keyed by conductor name (the string passed to `agent-deck conductor setup <name>`). Precedence (high → low): `CLAUDE_CONFIG_DIR` env > `[conductors.<name>.claude]` > `[groups."<g>".claude]` > `[profiles.<p>.claude]` > `[claude]` > `~/.claude`.

Closes [issue #602](https://github.com/asheshgoplani/agent-deck/issues/602).

**Canonical vs pool:** This SKILL.md lives at `~/.claude/plugins/cache/agent-deck/agent-deck/<hash>/skills/agent-deck/SKILL.md` (auto-loaded by Claude Code when the agent-deck plugin is active). A pool copy at `~/.agent-deck/skills/pool/agent-deck/SKILL.md` exists on some hosts — the pool version is loaded on-demand via `Read ~/.agent-deck/skills/pool/agent-deck/SKILL.md`.
```

Write via the Write tool — not a shell heredoc. Capture a diff BEFORE/AFTER (see Step 2e).

**Step 2d — Attempt pool update.**
```bash
ls -la ~/.agent-deck/skills/pool/agent-deck/SKILL.md 2>/dev/null
```
If the file exists, insert the same paragraph (minus the "Canonical vs pool" note, which is optional in the pool copy — the pool copy already IS the pool). If absent, document the absence in the SKILL_MD_DIFF.md artifact (Step 2e) — the spec says "pool path if present" (REQUIREMENTS.md:68), so absence is not a gap, but the audit artifact MUST record it.

**Step 2e — MANDATORY audit artifact: write `SKILL_MD_DIFF.md`.** This is a repo-visible record of what changed in the external SKILL.md files (or what was absent). Path:
```
.planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md
```

The artifact MUST exist regardless of plugin cache presence. Required structure:

```markdown
# SKILL.md Updates — Phase 4 Audit Artifact (CFG-09)

Captures the external-file updates applied during Plan 04-02 Task 2. External
paths are not part of the repo working tree, so this artifact provides the
repo-visible audit trail required for CFG-09 closure.

## Canonical plugin-cache SKILL.md

**Path resolved:** `<full path or "NOT FOUND">`

**Status:** `applied` | `no canonical skill found — host lacks plugin cache directory; SKILL.md update skipped`

**Diff applied** (unified format, context-3, or the literal skip string):

\`\`\`diff
<exact unified diff from `diff -u /tmp/skill.before.md <canonical>/SKILL.md`>
\`\`\`

## Pool SKILL.md

**Path:** `~/.agent-deck/skills/pool/agent-deck/SKILL.md`

**Status:** `applied` | `no pool skill found — host lacks pool directory; SKILL.md update skipped`

**Diff applied** (unified format, or the literal skip string):

\`\`\`diff
<exact unified diff, or skip string>
\`\`\`

## Verification

- `grep "\[conductors\..*\.claude\]" <path>` output captured below each section.
- Phase 4 plan-02 acceptance criteria require this artifact exists, is non-empty, and has BOTH sections populated (either with a diff OR the literal skip string).
```

**Capture diffs BEFORE editing.** Best practice:
```bash
CANONICAL_DIR=$(ls -dt ~/.claude/plugins/cache/agent-deck/agent-deck/*/skills/agent-deck/ 2>/dev/null | head -1)
if [ -n "$CANONICAL_DIR" ] && [ -f "$CANONICAL_DIR/SKILL.md" ]; then
  cp "$CANONICAL_DIR/SKILL.md" /tmp/skill.canonical.before.md
fi
if [ -f ~/.agent-deck/skills/pool/agent-deck/SKILL.md ]; then
  cp ~/.agent-deck/skills/pool/agent-deck/SKILL.md /tmp/skill.pool.before.md
fi
# ... apply edits ...
diff -u /tmp/skill.canonical.before.md "$CANONICAL_DIR/SKILL.md" > /tmp/skill.canonical.diff
diff -u /tmp/skill.pool.before.md ~/.agent-deck/skills/pool/agent-deck/SKILL.md > /tmp/skill.pool.diff
# Then write SKILL_MD_DIFF.md embedding the diff contents (or skip strings).
```

**Step 2f — Commit the audit artifact.** The SKILL.md files themselves live OUTSIDE the repo and don't produce a repo commit. The SKILL_MD_DIFF.md artifact DOES ship in the repo and is committed with hooks enabled (it's under `.planning/**` but this commit is scoped to the audit artifact + no source — hooks will no-op, no `--no-verify` needed):

```
docs(04): record SKILL.md external-file updates (CFG-09 audit artifact)

Captures the diffs applied to canonical plugin-cache and pool SKILL.md
paths (or documents their absence on this execution host). Required
by Phase 4 Plan 04-02 — SKILL.md files live outside the repo working
tree, so this artifact is the repo-visible audit trail for CFG-09
closure.

Refs: https://github.com/asheshgoplani/agent-deck/issues/602

Committed by Ashesh Goplani
```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md && test -s .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md && grep -q "Canonical plugin-cache\|Pool SKILL" .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md` exists AND is non-empty (mandatory regardless of plugin cache presence)
    - `SKILL_MD_DIFF.md` contains BOTH `Canonical plugin-cache SKILL.md` and `Pool SKILL.md` section headings
    - `SKILL_MD_DIFF.md` Canonical section contains either a `\`\`\`diff` block OR the literal string `no canonical skill found — host lacks plugin cache directory; SKILL.md update skipped`
    - `SKILL_MD_DIFF.md` Pool section contains either a `\`\`\`diff` block OR the literal string `no pool skill found — host lacks pool directory; SKILL.md update skipped`
    - IF canonical SKILL.md exists on host (`ls -dt ~/.claude/plugins/cache/agent-deck/agent-deck/*/skills/agent-deck/ | head -1` returns a path): that file contains `[conductors.<name>.claude]` OR `conductors.<n>.claude`, contains `Per-conductor Claude config` or `Per-conductor`, and contains `issues/602`
    - IF pool SKILL.md exists on host: same three substrings present
    - Commit subject matches `^docs\(04\): record SKILL.md external-file updates \(CFG-09 audit artifact\)$`
    - Commit body contains `https://github.com/asheshgoplani/agent-deck/issues/602` and `Committed by Ashesh Goplani`
    - No `@alec-pinson` and no `Co-Authored-By: Claude` in commit body
  </acceptance_criteria>
  <done>
    Canonical SKILL.md documents the new schema (when host has plugin cache). Pool SKILL.md updated if present. The MANDATORY repo-visible `SKILL_MD_DIFF.md` audit artifact is committed with both canonical-and-pool sections populated (either a diff or the explicit skip string). CFG-09 closure is auditable even when external paths are unavailable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add `--no-verify` bullet + scope clarification sub-section to repo-root CLAUDE.md `## General rules`</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (MANDATORY — read the "## General rules" section in full to understand exact heading level, tone, and to CONFIRM that NO `--no-verify` bullet exists yet)
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-02-SUMMARY.md (Phase 3 touched CLAUDE.md; confirm what the current state is — `## General rules` exists from Phase 3's resurrection work but has no `--no-verify` line)
    - docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md (REQ-10 / CFG-10 lines 162-171 — the canonical rule text)
    - .planning/REQUIREMENTS.md (CFG-10 lines 70-71)
  </read_first>
  <action>
**Step 3a — Pre-flight verification (sanity check).** Before editing, confirm the current state:
```bash
grep -n "General rules" CLAUDE.md
grep -n "no-verify\|--no-verify" CLAUDE.md
```
Expected:
- `grep -n "General rules"` returns a hit (the section exists — shipped by Phase 3 plan 03-02).
- `grep -n "no-verify"` returns **ZERO matches** — the `--no-verify` bullet does NOT currently exist in the file. This plan ESTABLISHES it.

If `grep -n "no-verify"` returns a match (contradicting the plan-time check), STOP and re-review before editing. The plan assumes no bullet exists; if one was added between planning and execution, the executor must decide whether to augment it or rewrite — do not blindly duplicate.

**Step 3b — Add the `--no-verify` bullet to `## General rules`.** Locate the existing bullet list under `## General rules (inherited from user global)` — current bullets are: `Never \`rm\``, `Never commit with Claude attribution`, `Never \`git push\`...`, `TDD always`, `Simplicity first`.

Insert a NEW bullet immediately after the `Never \`git push\`...` bullet (grouping it with the commit-hygiene bullets) OR at the end of the bullet list (grouping it with the workflow bullets). Choose based on tone — the `git push` bullet is about commit operations, so place the new bullet right after it:

```markdown
- **No `--no-verify`** — every commit goes through pre-commit hooks. EXCEPTION: metadata-only commits — see `### --no-verify scope clarification` below.
```

**Step 3c — Insert the clarification sub-section.** Immediately AFTER the `## General rules` bullet list (before the next `##`-level heading — currently `## Current milestone`), insert:

```markdown
### --no-verify scope clarification (v1.5.4+)

The `--no-verify` ban above applies to **source-modifying commits only**. Commits that touch ONLY metadata paths MAY use `--no-verify` iff the pre-commit hook would no-op. This clarification prevents paying 10–30s of hook latency on commits that provably don't require verification.

**Metadata-only paths (exempt from the ban):**

- `.planning/**` — phase plans, summaries, state, requirements
- `docs/**` — specs, design docs, architecture notes
- `*.md` outside `internal/`, `cmd/`, `pkg/`, and other source dirs
- `CHANGELOG.md` during milestone-prep phases

**Source-modifying commits (ban still applies):**

- Any commit that modifies `internal/**/*.go`, `cmd/**/*.go`, `pkg/**/*.go`, `scripts/**`, or any file under `internal/tmux/**`
- Any commit that modifies `go.mod`, `go.sum`, or CI config
- Repo-root `CLAUDE.md` and `README.md` (these affect development workflow and deployment)

**Negative example.** A commit staging `.planning/STATE.md` AND `internal/session/instance.go` is a SOURCE-modifying commit (source files present in the staged set). Hooks MUST run. You cannot bypass hooks by arguing the metadata file is the "primary" change.

**Positive example.** A commit staging ONLY `.planning/phases/04-*/04-01-SUMMARY.md` and `.planning/STATE.md` is metadata-only. Hooks would no-op (no Go source changed, no lint-relevant files). `--no-verify` is acceptable here.

Rationale: preserves the v1.5.3 test-gate for source changes while freeing metadata commits from hook latency. Phase 4 (v1.5.4) of this milestone established both the `--no-verify` ban and this clarification (see issue #602 for the conductor block that drove the Phase 4 volume of metadata commits).
```

**Step 3d — Commit.** Repo-root `CLAUDE.md` is treated as source per the clarification itself (see "Source-modifying commits" bullet above — CLAUDE.md affects development workflow). So this commit runs hooks. No `--no-verify`.

```
docs(04): add --no-verify ban + scope clarification to CLAUDE.md (CFG-10)

Establishes the --no-verify ban as a new bullet in ## General rules
(it did not exist in the file before this commit — verified via
`grep -n "no-verify" CLAUDE.md` returning zero matches pre-edit) AND
adds a "--no-verify scope clarification" sub-section listing
metadata-only paths exempt from the v1.5.3 hook-run mandate.
Source-modifying commits (including this one — CLAUDE.md is itself
source-modifying) continue to require hooks. Includes positive and
negative examples.

The clarification was driven by Phase 4's volume of metadata commits
(spec/roadmap/state/requirements amendments for issue #602) where the
pre-commit hook consistently no-ops and costs 10–30s per commit.

Committed by Ashesh Goplani
```

Note: This commit body does not need to reference #602 directly (the README commit covers that must-have). But mentioning the driving context is natural and helpful.
  </action>
  <verify>
    <automated>
      grep -q "No \`--no-verify\`" CLAUDE.md && grep -q "no-verify scope clarification\\|--no-verify scope" CLAUDE.md && grep -q "Metadata-only paths" CLAUDE.md && grep -q "Negative example" CLAUDE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "No \`--no-verify\`" CLAUDE.md` (the NEW bullet is present)
    - `grep -c "no-verify" CLAUDE.md` returns at least 2 (one in the bullet, one or more in the clarification sub-section)
    - `grep -q "no-verify scope clarification" CLAUDE.md`
    - `grep -q "Metadata-only paths" CLAUDE.md`
    - `grep -q "\\.planning/\\*\\*\\|\\.planning/" CLAUDE.md` (metadata path list)
    - `grep -q "docs/\\*\\*\\|docs/" CLAUDE.md` (metadata path list includes docs)
    - `grep -q "Negative example\\|negative example" CLAUDE.md`
    - `grep -q "Positive example\\|positive example" CLAUDE.md`
    - `grep -q "Source-modifying" CLAUDE.md`
    - `grep -q "metadata-only" CLAUDE.md`
    - The new `--no-verify` bullet appears inside the `## General rules` section — verify: `awk '/^## General rules/,/^## /' CLAUDE.md | grep -q "No \`--no-verify\`"`
    - Git log shows commit with subject `^docs\(04\): add --no-verify ban \+ scope clarification to CLAUDE.md \(CFG-10\)$`
    - Commit body contains `Committed by Ashesh Goplani`
    - Commit body does NOT contain `@alec-pinson` or `Co-Authored-By: Claude`
    - Hooks RAN on this commit (CLAUDE.md is source per the clarification itself). Verify operationally.
  </acceptance_criteria>
  <done>
    Repo-root CLAUDE.md gains a NEW `**No `--no-verify`**` bullet in `## General rules` (establishing the base rule, which did not exist before) AND a new `### --no-verify scope clarification` sub-section with metadata-path list, positive example, negative example, and rationale. Commit signed "Committed by Ashesh Goplani" without external attributions. Ran with hooks enabled.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase 4 hard-rule audit + SUMMARY.md + roadmap/state cleanup</name>
  <files>.planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md, .planning/STATE.md (possibly), .planning/REQUIREMENTS.md (possibly)</files>
  <read_first>
    - .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-01-SUMMARY.md (to link commit hashes)
    - .planning/STATE.md (to find the "## Phase 04 commits" table that needs filling in)
    - .planning/REQUIREMENTS.md (CFG-09, CFG-10, CFG-11 checkbox states — flip to [x] if criteria met)
  </read_first>
  <action>
**Step 4a — Hard-rule audit.** Run the following audit commands, capture each result into `/tmp/phase4-audit.log`:

```bash
# The commit range for Phase 4 starts at the Plan 04-01 Task 1 RED commit.
# Capture it via:
PHASE4_START_HASH=$(git log --grep "test(04): add conductor config regression tests" --format=%H -n 1)
PHASE4_START_SHORT=$(git log --format=%h "$PHASE4_START_HASH" -n 1)

echo "Phase 4 start commit: $PHASE4_START_HASH" | tee /tmp/phase4-audit.log

# Audit 1: No @alec-pinson in any Phase 4 commit body.
ALEC_COUNT=$(git log "$PHASE4_START_HASH^..HEAD" --format=%B | grep -c "@alec-pinson")
echo "Audit 1 @alec-pinson count: $ALEC_COUNT (MUST be 0)" | tee -a /tmp/phase4-audit.log

# Audit 2: No Claude attribution in any Phase 4 commit body.
CLAUDE_ATTRIB=$(git log "$PHASE4_START_HASH^..HEAD" --format=%B | grep -c "Co-Authored-By: Claude\|Generated with Claude")
echo "Audit 2 Claude attribution count: $CLAUDE_ATTRIB (MUST be 0)" | tee -a /tmp/phase4-audit.log

# Audit 3: Every Phase 4 commit signed "Committed by Ashesh Goplani".
PHASE4_COMMIT_COUNT=$(git rev-list "$PHASE4_START_HASH^..HEAD" | wc -l)
SIGNED_COUNT=$(git log "$PHASE4_START_HASH^..HEAD" --format=%B | grep -c "Committed by Ashesh Goplani")
echo "Audit 3 signed/total: $SIGNED_COUNT/$PHASE4_COMMIT_COUNT (MUST match)" | tee -a /tmp/phase4-audit.log

# Audit 4: At least one Phase 4 commit references issue #602.
REF_602=$(git log "$PHASE4_START_HASH^..HEAD" --format=%B | grep -c "#602\|issues/602")
echo "Audit 4 #602 reference count: $REF_602 (MUST be >= 1)" | tee -a /tmp/phase4-audit.log

# Audit 5: Final test sweep.
go test ./internal/session/... -run "TestConductorConfig_|TestPerGroupConfig_" -race -count=1 2>&1 | tail -5 | tee -a /tmp/phase4-audit.log
```

If any audit fails:
- Audit 1/2 fail → STOP. The planner expects zero @alec-pinson or Claude attribution. Fix by amending the offending commit (use `git rebase -i $PHASE4_START_HASH^` or, if the commit is HEAD, a fresh commit that explicitly retracts the attribution — the repo CLAUDE.md says prefer new commits over amend, but for attribution correctness amend is acceptable after confirming with the user. DO NOT `--force push` — local only.)
- Audit 3 fail → add a follow-up commit with an explicit `Committed by Ashesh Goplani` trailer. Do not amend prior commits just for the signature.
- Audit 4 fail → add a follow-up commit that touches SUMMARY.md with an `Refs: https://github.com/asheshgoplani/agent-deck/issues/602` body line.
- Audit 5 fail → STOP. Test regression. Do not proceed to SUMMARY.

**Step 4b — Write `04-02-SUMMARY.md`** using the GSD summary template. Include:
- **Plan 04-02 commit log:** README commit, SKILL_MD_DIFF audit-artifact commit, CLAUDE.md mandate commit, and any audit follow-up commits. Note that external SKILL.md files live outside the repo but the SKILL_MD_DIFF.md artifact is the repo-visible record.
- **CFG-09 closure:** README extended, SKILL.md canonical updated (or absent — recorded in audit artifact), pool SKILL.md updated (or documented absent in audit artifact).
- **CFG-10 closure:** CLAUDE.md `--no-verify` bullet established (did not exist before) + scope clarification sub-section shipped with positive + negative examples.
- **Hard-rule audit results** (from `/tmp/phase4-audit.log`) — all four pass.
- **Phase 4 milestone close-out status:** With Plan 04-01 and 04-02 complete, CFG-08, CFG-09, CFG-10, CFG-11 all closed. Success criteria 7, 8, 9, 10, 11, 12 from the milestone spec are ready for verification at `/gsd-complete-milestone`.
- **Deviations and rationale:** Nested `[conductors.<name>.claude]` TOML shape (vs spec's ambiguous flat example) — chosen to mirror group pattern for consistency.

**Step 4c — Update `.planning/STATE.md`** to add the Phase 4 commits table under the existing "Phase 03 commits" section:

```markdown
## Phase 04 commits (since parent plan 03-02)

| Hash | Type | Subject |
|------|------|---------|
| <hash> | test | test(04): add conductor config regression tests (RED, CFG-11) |
| <hash> | feat | feat(04): add [conductors.<name>.claude] schema (CFG-08 partial) |
| <hash> | feat | feat(04): wire conductor-block loader + four callsites (CFG-08) |
| <hash> | (fix/test) | ... (any task 4a/4b/4c commits from plan 04-01) |
| <hash> | docs | docs(04): document [conductors.<name>.claude] schema in README (CFG-09) |
| <hash> | docs | docs(04): record SKILL.md external-file updates (CFG-09 audit artifact) |
| <hash> | docs | docs(04): add --no-verify ban + scope clarification to CLAUDE.md (CFG-10) |
```

Fill in hashes from `git log --format="%h | %s"` filtered to Phase 4's range.

Also update the "Current Position" block:
```
Phase: 04
Plan: Complete (04-01 + 04-02)
Status: Phase 04 complete; milestone ready for /gsd-complete-milestone
```

And flip the Phase 4 row in the progress table to `Complete`.

**Step 4d — Update `.planning/REQUIREMENTS.md`:**
Flip `[ ]` to `[x]` on CFG-09, CFG-10, CFG-11 (and CFG-08 if not already flipped by Plan 04-01 SUMMARY).

Update the Traceability table:
- CFG-08 | Phase 4 | **Complete (04-01)**
- CFG-09 | Phase 4 | **Complete (04-02)**
- CFG-10 | Phase 4 | **Complete (04-02)**
- CFG-11 | Phase 4 | **Complete (04-01)**

**Step 4e — Commit** the metadata updates. This commit stages ONLY `.planning/**` files (SUMMARY + STATE + REQUIREMENTS). Per the CFG-10 clarification shipped in Task 3, this qualifies for `--no-verify`. **However, this plan DEFAULTS to plain `git commit` (hooks enabled)** for two reasons:
1. **Hooks will no-op** on a `.planning/**`-only staged set — there is no latency to save.
2. **Safety during CFG-10 rollout.** Using `--no-verify` on the very commit that records the CFG-10 clarification would normalize the exemption before the executor has internalized the rule. Defaulting to hooks-enabled keeps the discipline visible.

The CFG-10 clarification PERMITS `--no-verify` here but this plan does NOT use it. If a future plan wants to demonstrate the exemption in action, it can do so on a later metadata-only commit.

```bash
git add .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md .planning/STATE.md .planning/REQUIREMENTS.md
git commit -m "$(cat <<'EOF'
docs(04): close Phase 4 — SUMMARY, STATE, REQUIREMENTS (metadata-only)

Records Phase 4 completion: CFG-08 + CFG-11 closed in plan 04-01,
CFG-09 + CFG-10 closed in plan 04-02. All four audit gates pass
(no @alec-pinson, no Claude attribution, all commits signed,
issue #602 referenced).

Metadata-only commit. Per the CFG-10 clarification shipped in
commit <CLAUDE.md commit hash>, --no-verify would be permitted for
this staged set (`.planning/**` only). This plan defaults to plain
`git commit` (hooks enabled) — hooks no-op on metadata-only staging
so there is no latency cost, and keeping hooks visible during the
CFG-10 rollout prevents premature normalization of the exemption.

Committed by Ashesh Goplani
EOF
)"
```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md && grep -q "CFG-09\|CFG-10" .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md && grep -q "Phase 04 commits" .planning/STATE.md && grep -q "Complete (04-01)\|Complete (04-02)" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md` exits 0
    - SUMMARY file references CFG-09 and CFG-10 closure
    - SUMMARY file records the four audit results (@alec-pinson=0, Claude attribution=0, all signed, #602 referenced)
    - SUMMARY file notes the nested TOML shape deviation from spec's flat example (rationale: schema mirrors group pattern)
    - SUMMARY file mentions the SKILL_MD_DIFF.md audit artifact and its contents (applied diffs vs skip strings)
    - `.planning/STATE.md` has a "Phase 04 commits" section with hashes for all Phase 4 commits
    - `.planning/STATE.md` "Current Position" block shows Phase 04 complete
    - `.planning/REQUIREMENTS.md` CFG-09, CFG-10, CFG-11 all flipped to `[x]`
    - `.planning/REQUIREMENTS.md` Traceability table shows CFG-08/09/10/11 as Complete
    - Audit log confirms:
      - `grep "@alec-pinson count: 0" /tmp/phase4-audit.log`
      - `grep "Claude attribution count: 0" /tmp/phase4-audit.log`
      - `grep "signed/total" /tmp/phase4-audit.log` shows matching counts
      - `grep "#602 reference count" /tmp/phase4-audit.log` shows ≥ 1
    - Final test sweep in `/tmp/phase4-audit.log`: 8/8 PASS for `TestConductorConfig_` AND 8/8 PASS for `TestPerGroupConfig_`
    - Final commit has subject `^docs\(04\): close Phase 4 —` (or equivalent)
    - Final commit was made WITHOUT `--no-verify` (hooks ran, or no-op'd on metadata-only staging)
  </acceptance_criteria>
  <done>
    Phase 4 metadata records complete: SUMMARY.md shipped, STATE.md commit table + progress flipped, REQUIREMENTS.md checkboxes flipped, hard-rule audit passed with zero violations. Final commit uses plain `git commit` (hooks enabled) — CFG-10 permits `--no-verify` here but this plan defaults to hooks to avoid normalizing the exemption on the rollout commit. Milestone is ready for `/gsd-complete-milestone`.
  </done>
</task>

</tasks>

<verification>
**CFG-09 (docs refresh):**
- `grep -q "Per-conductor Claude config" README.md`
- `grep -q "\\[conductors\\..*\\.claude\\]" README.md` (nested form)
- `grep -q "issues/602" README.md`
- `test -s .planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/SKILL_MD_DIFF.md` (MANDATORY audit artifact, non-empty)
- Canonical SKILL.md at `~/.claude/plugins/cache/agent-deck/agent-deck/<hash>/skills/agent-deck/SKILL.md` contains the conductor-block paragraph (or absence recorded in SKILL_MD_DIFF.md)
- Pool SKILL.md updated if present (absence recorded in SKILL_MD_DIFF.md if not)

**CFG-10 (mandate + clarification):**
- `grep -q "No \`--no-verify\`" CLAUDE.md` (the base ban bullet is NEW — did not exist before this plan)
- `grep -q "no-verify scope clarification" CLAUDE.md`
- `grep -q "Metadata-only paths" CLAUDE.md`
- `grep -q "Source-modifying" CLAUDE.md`
- `grep -q "Positive example\\|Negative example" CLAUDE.md`

**Phase 4 audit (final gate):**
- Zero `@alec-pinson` in Phase 4 commit bodies
- Zero `Co-Authored-By: Claude` in Phase 4 commit bodies
- Every Phase 4 commit signed `Committed by Ashesh Goplani`
- At least one Phase 4 commit body references issue #602
- `go test ./internal/session/... -run "TestConductorConfig_|TestPerGroupConfig_" -race -count=1` exits 0 (16/16 PASS)
</verification>

<success_criteria>
1. README.md `### Per-group Claude config` subsection extended with sibling `#### Per-conductor Claude config` block documenting the nested `[conductors.<name>.claude]` schema, precedence chain, backward-compat note, and link to issue #602.
2. Canonical agent-deck SKILL.md at the plugin-cache path updated with the conductor-block paragraph; pool SKILL.md updated if present; MANDATORY `SKILL_MD_DIFF.md` audit artifact shipped in `.planning/phases/04-*/` regardless of external-file availability.
3. Repo-root CLAUDE.md has a NEW `**No `--no-verify`**` bullet in `## General rules` (the base ban did not exist before this plan) AND a new `### --no-verify scope clarification` sub-section with metadata-paths list, positive + negative examples, and rationale.
4. Hard-rule audit confirms: zero `@alec-pinson`, zero Claude attribution, all signed, ≥1 #602 reference across Phase 4.
5. `04-02-SUMMARY.md` written with all four audit results, the nested-TOML shape rationale, and SKILL_MD_DIFF.md reference.
6. `.planning/STATE.md` Phase 04 commits table populated; progress row flipped to Complete.
7. `.planning/REQUIREMENTS.md` CFG-09, CFG-10, CFG-11 checkboxes flipped; Traceability rows marked Complete.
8. Both test suites still GREEN: `TestConductorConfig_` 8/8 + `TestPerGroupConfig_` 8/8 (final regression gate).
9. Final metadata commit uses plain `git commit` (hooks enabled) — does NOT use `--no-verify` (avoids normalizing the exemption on the CFG-10 rollout commit).
</success_criteria>

<output>
After completion, create `.planning/phases/04-conductor-schema-docs-refresh-mandate-clarification/04-02-SUMMARY.md` capturing commit hashes, CFG-09/CFG-10 closure status, Phase 4 hard-rule audit results, the nested-TOML shape rationale, and SKILL_MD_DIFF.md audit artifact reference. Update STATE.md and REQUIREMENTS.md. This plan closes the milestone and unblocks `/gsd-complete-milestone`.
</output>
