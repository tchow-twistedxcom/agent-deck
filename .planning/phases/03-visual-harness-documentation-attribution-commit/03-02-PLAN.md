---
phase: 03-visual-harness-documentation-attribution-commit
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - README.md
  - CLAUDE.md
  - CHANGELOG.md
autonomous: true
requirements:
  - CFG-06
user_setup: []

must_haves:
  truths:
    - "`README.md` contains a new `Per-group Claude config` subsection with the locked TOML example `[groups.\"conductor\".claude]` / `config_dir = \"~/.claude-work\"` / `env_file = \"~/git/work/.envrc\"` and a pointer to `scripts/verify-per-group-claude-config.sh`."
    - "Repo-root `CLAUDE.md` exists (Path A resurrection from `5013940^:CLAUDE.md`) and contains the verbatim one-liner `Per-group config dir applies to custom-command sessions too; \\`TestPerGroupConfig_*\\` suite enforces this.` under a session-persistence mandate block."
    - "`CHANGELOG.md` `[Unreleased]` block has an `### Added` subsection containing the bullet `- Per-group Claude config overrides (\\`[groups.\"<name>\".claude]\\`).` with the PR #578 reference appended."
    - "`git log main..HEAD --grep '@alec-pinson'` returns ≥ 2 commits (Plan 01 harness commit + at least one doc commit from this plan)."
    - "No commit on `main..HEAD` contains `Generated with Claude Code` or `Co-Authored-By: Claude`."
    - "No `git push`, `git tag`, `gh release`, `gh pr create`, or `gh pr merge` was executed anywhere in this phase."
  artifacts:
    - path: "README.md"
      provides: "`### Per-group Claude config` subsection with TOML example + harness pointer + priority summary"
      contains: "Per-group Claude config"
    - path: "CLAUDE.md"
      provides: "Repo-root agent mandate file with session-persistence block containing the TestPerGroupConfig_* enforcement one-liner"
      contains: "TestPerGroupConfig_"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] > Added bullet for per-group Claude config overrides"
      contains: "Per-group Claude config overrides"
  key_links:
    - from: "README.md"
      to: "scripts/verify-per-group-claude-config.sh"
      via: "markdown link in the new subsection body"
      pattern: "scripts/verify-per-group-claude-config.sh"
    - from: "CLAUDE.md"
      to: "internal/session/pergroupconfig_test.go"
      via: "one-line reference to the TestPerGroupConfig_* suite"
      pattern: "TestPerGroupConfig_"
    - from: "CHANGELOG.md"
      to: "github.com/asheshgoplani/agent-deck/pull/578"
      via: "PR #578 link appended to the Added bullet"
      pattern: "\\[PR #578\\]"
    - from: "git log main..HEAD"
      to: "@alec-pinson"
      via: "at least one commit body carries `Base implementation by @alec-pinson in PR #578.`"
      pattern: "@alec-pinson"
---

<objective>
Close CFG-06 by landing all three doc surfaces (`README.md` subsection, repo-root `CLAUDE.md` one-liner, `CHANGELOG.md` `[Unreleased] > Added` bullet) and ensuring at least one commit on `main..HEAD` carries `@alec-pinson` attribution in its body. Plan 01 already landed one attribution commit (the harness); this plan adds at least one more via the doc commits and runs a final audit task that verifies all milestone hard rules still hold.

Purpose: Three doc commits + one audit task complete the phase. Docs reference the harness by name — Plan 01 MUST be complete and GREEN before this plan runs. No code changes. No test changes. `TestPerGroupConfig_*` mention in CLAUDE.md is a factual claim about Phase 1+2 output; the CLAUDE.md resurrection path (Path A from `5013940^`) is locked in CONTEXT.md.

Output: Three doc commits (README / CLAUDE.md / CHANGELOG), each carrying `Base implementation by @alec-pinson in PR #578.` in its body and `Committed by Ashesh Goplani` trailer. A final `03-02-SUMMARY.md` captures the hard-rule audit output proving no push/tag/PR/merge/Claude-attribution violation occurred at any point on `main..HEAD`.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/per-group-claude-config/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/per-group-claude-config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visual-harness-documentation-attribution-commit/03-CONTEXT.md
@.planning/phases/03-visual-harness-documentation-attribution-commit/03-01-PLAN.md
@docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md
@README.md
@CHANGELOG.md

<interfaces>
<!-- Locked verbatim strings from CONTEXT.md §decisions. DO NOT rewrite. -->

README subsection title:
```
### Per-group Claude config
```

README TOML example (verbatim from spec):
```toml
[groups."conductor".claude]
config_dir = "~/.claude-work"
env_file = "~/git/work/.envrc"
```

README priority-summary one-liner (matches CFG-07 log source labels):
```
Priority: env > group > profile > global > default.
```

CLAUDE.md one-liner (verbatim from spec):
```
Per-group config dir applies to custom-command sessions too; `TestPerGroupConfig_*` suite enforces this.
```

CHANGELOG bullet (verbatim):
```markdown
- Per-group Claude config overrides (`[groups."<name>".claude]`). (Base implementation by @alec-pinson in [PR #578](https://github.com/asheshgoplani/agent-deck/pull/578))
```

Commit attribution trailer (verbatim):
```
Base implementation by @alec-pinson in PR #578.
```

Commit signature trailer (verbatim):
```
Committed by Ashesh Goplani
```

<!-- README insertion point (confirmed via planner read): -->
<!--   Insert `### Per-group Claude config` as a new §Features subsection, positioned AFTER `### Skills Manager` (ends at L96) and BEFORE `### MCP Socket Pool` (starts at L98). Zero anchor churn. -->

<!-- CLAUDE.md resurrection source (Path A): -->
<!--   `git show 5013940^:CLAUDE.md` returns the 460-line v1.5.2 CLAUDE.md with `## Session persistence: mandatory test coverage` at line ~5 (confirmed via `git show a262c6d:CLAUDE.md`). -->
<!--   Fallback: `git show fb7caad:CLAUDE.md` (147-line v1.5.3 scoped — missing session-persistence heading; use only if 5013940^ path is blocked). -->

<!-- CHANGELOG insertion point: -->
<!--   `## [Unreleased]` is at line 8 and currently empty. Add `### Added` as a sibling sub-heading directly under it, then the bullet. No renumbering. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add README "Per-group Claude config" subsection under §Features</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 73-135 — §Features section; locate the seam between `### Skills Manager` (ends ~L96) and `### MCP Socket Pool` (starts ~L98))
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-CONTEXT.md (§CFG-06 README subsection — locked content + insertion-point rule: "no anchor churn")
    - docs/PER-GROUP-CLAUDE-CONFIG-SPEC.md (the TOML example block must be lifted verbatim from the spec; do NOT paraphrase)
  </read_first>
  <action>
Insert the following subsection into `README.md` between the `### Skills Manager` block (which ends with "Type-to-jump is supported in the dialog (same pattern as MCP Manager)") and the `### MCP Socket Pool` heading. Insertion point: one blank line after `### Skills Manager`'s last bullet, before the blank line preceding `### MCP Socket Pool`.

Content to insert (verbatim — markdown indentation and backticks as shown):

```markdown
### Per-group Claude config

Agent Deck supports per-group `CLAUDE_CONFIG_DIR` and `env_file` overrides. Useful when a single profile hosts groups that should authenticate against different Claude accounts — for example, a personal profile hosting a `conductor` group pinned to `~/.claude-work` while other groups stay on `~/.claude`.

Override any group by adding a `[groups."<name>".claude]` table to `~/.agent-deck/config.toml`:

```toml
[groups."conductor".claude]
config_dir = "~/.claude-work"
env_file = "~/git/work/.envrc"
```

Lookup priority: `env > group > profile > global > default`. The `env_file` is `source`d into the tmux pane before `claude` (or the custom command) execs, so any exports it contains become part of the session environment.

Human-watchable verification: `bash scripts/verify-per-group-claude-config.sh`. The harness creates two throwaway groups, launches one normal and one custom-command session, and prints a pass/fail table.

```

Concrete diff semantics:
- Add exactly ONE new `### Per-group Claude config` subsection.
- Do NOT renumber, reshuffle, or touch any other subsection.
- Do NOT modify the TOC (the README has no explicit TOC; no link fixups needed).
- Keep the fenced TOML block's opening line as literally ` ```toml ` and closing as ` ``` `.

After the edit, stage and commit:

```bash
git add README.md
git commit -m "$(cat &lt;&lt;'EOF'
docs(readme): add Per-group Claude config subsection (CFG-06)

Adds a new `### Per-group Claude config` subsection under §Features,
sibling to `### Skills Manager` and `### MCP Socket Pool`. Explains the
`[groups."<name>".claude]` override, quotes the TOML example verbatim
from the spec, documents the lookup priority (env > group > profile >
global > default), and points readers at `scripts/verify-per-group-claude-config.sh`
for human-watchable verification.

No anchor churn — existing section order (Problem / Solution / Features /
Installation / Quick Start / Documentation / FAQ / Development / Star
History / License) is preserved.

Base implementation by @alec-pinson in PR #578.

Refs: CFG-06, REQ-6
Committed by Ashesh Goplani
EOF
)"
```

Hard rules: NO `--no-verify`, NO push/tag/PR/merge, NO Claude attribution in the commit message. If `lefthook` rejects (e.g., markdown linter), fix the flagged issue (most likely trailing whitespace or line-length) and re-commit with the same heredoc.
  </action>
  <verify>
    <automated>grep -c "^### Per-group Claude config$" README.md | grep -q "^1$" &amp;&amp; grep -q '\[groups."conductor".claude\]' README.md &amp;&amp; grep -q 'config_dir = "~/.claude-work"' README.md &amp;&amp; grep -q 'env_file = "~/git/work/.envrc"' README.md &amp;&amp; grep -q "scripts/verify-per-group-claude-config.sh" README.md &amp;&amp; grep -q "env &gt; group &gt; profile &gt; global &gt; default" README.md &amp;&amp; git log -1 --pretty=%s HEAD | grep -q "^docs(readme): add Per-group Claude config subsection (CFG-06)$" &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Base implementation by @alec-pinson in PR #578." &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Committed by Ashesh Goplani" &amp;&amp; ! git log -1 --pretty=%B HEAD | grep -qE "Generated with Claude|Co-Authored-By: Claude"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Per-group Claude config$" README.md` returns exactly `1`.
    - `grep -c 'Per-group Claude config' README.md` returns ≥ 1.
    - README contains the verbatim TOML fragment `[groups."conductor".claude]`.
    - README contains `config_dir = "~/.claude-work"` AND `env_file = "~/git/work/.envrc"`.
    - README contains a reference to `scripts/verify-per-group-claude-config.sh`.
    - README contains the priority summary `env > group > profile > global > default`.
    - `git log -1 --pretty=%s HEAD` exactly equals `docs(readme): add Per-group Claude config subsection (CFG-06)`.
    - `git log -1 --pretty=%b HEAD` contains `Base implementation by @alec-pinson in PR #578.`.
    - `git log -1 --pretty=%b HEAD` contains `Committed by Ashesh Goplani`.
    - `git log -1 --pretty=%B HEAD` does NOT contain `Generated with Claude` or `Co-Authored-By: Claude`.
    - `git diff-tree --no-commit-id --name-only -r HEAD` lists exactly `README.md`.
  </acceptance_criteria>
  <done>README subsection inserted at the chosen seam, commit landed with attribution trailer, all acceptance greps pass.</done>
</task>

<task type="auto">
  <name>Task 2: Resurrect repo-root CLAUDE.md via Path A and add the TestPerGroupConfig_* enforcement one-liner</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-CONTEXT.md (§CFG-06 repo-root CLAUDE.md one-liner + claude_md_handling — Path A/B/C decision matrix is LOCKED; Path A is the recommended and expected path)
    - .planning/REQUIREMENTS.md (CFG-06, L51: exact one-liner text)
    - The output of `git show 5013940^:CLAUDE.md | head -30` BEFORE writing the file (confirm the v1.5.2 CLAUDE.md with `## Session persistence: mandatory test coverage` heading is present; if missing, fall back to Path B)
  </read_first>
  <action>
Execute Path A (resurrect CLAUDE.md from `5013940^:CLAUDE.md`, then insert the one-liner, then commit with `-f` if needed).

Step 1 — confirm the resurrection source has the expected heading:
```bash
git show 5013940^:CLAUDE.md | grep -n "^## Session persistence: mandatory test coverage" | head -1
```
Expected: exactly one match on line ~5. If ZERO matches, fall back to Path B (create a minimal CLAUDE.md with the session-persistence heading synthesized fresh) and document the fallback in the commit body.

Step 2 — resurrect the file:
```bash
git show 5013940^:CLAUDE.md &gt; CLAUDE.md
```

Step 3 — confirm CLAUDE.md is NOT force-ignored:
```bash
git check-ignore -v CLAUDE.md
```
- If this prints nothing, the file is not ignored. Proceed.
- If this prints a path-ignored hit, the commit will need `git add -f CLAUDE.md`. Document the `-f` in the commit body.

Step 4 — insert the one-liner under the session-persistence mandate block. The v1.5.2 CLAUDE.md has `## Session persistence: mandatory test coverage` as a heading near line 5 followed by a bulleted list of mandates. Add the one-liner as a new bullet at the END of that section's bullet list (before the next `## ` heading appears). Use `awk` or hand-edit. The line to insert is verbatim:

```markdown
- Per-group config dir applies to custom-command sessions too; `TestPerGroupConfig_*` suite enforces this.
```

Concrete insertion approach (awk, idempotent): insert the bullet on the last line BEFORE the first subsequent `^## ` heading that follows `## Session persistence: mandatory test coverage`. If no bulleted list exists under that heading, insert the bullet directly after the heading, preceded by a blank line.

Step 5 — verify the bullet landed exactly once:
```bash
grep -c "TestPerGroupConfig_" CLAUDE.md
```
Expected: exactly `1`.

Step 6 — stage and commit. Use `git add -f CLAUDE.md` if Step 3 flagged the file as path-ignored; otherwise plain `git add CLAUDE.md`.

```bash
git add [-f] CLAUDE.md
git commit -m "$(cat &lt;&lt;'EOF'
docs(claude): resurrect repo-root CLAUDE.md and add TestPerGroupConfig_ enforcement one-liner (CFG-06)

CLAUDE.md was removed from tracking in commit 5013940 (`chore: remove CLAUDE.md from tracking (keep local)`) and has been untracked on both `main` and this branch since. CFG-06 requires a repo-root CLAUDE.md entry under the session-persistence mandate block, so this commit resurrects the pre-removal version from `5013940^:CLAUDE.md` (Path A per CONTEXT.md §claude_md_handling) and appends the locked one-liner:

> Per-group config dir applies to custom-command sessions too; `TestPerGroupConfig_*` suite enforces this.

The `TestPerGroupConfig_*` suite referenced here is the Phase 1 + Phase 2 output in `internal/session/pergroupconfig_test.go`:
- TestPerGroupConfig_CustomCommandGetsGroupConfigDir (Phase 1)
- TestPerGroupConfig_GroupOverrideBeatsProfile (Phase 1)
- TestPerGroupConfig_UnknownGroupFallsThroughToProfile (Phase 1)
- TestPerGroupConfig_EnvFileSourcedInSpawn (Phase 2 / plan 02-01)
- TestPerGroupConfig_ConductorRestartPreservesConfigDir (Phase 2 / plan 02-02)
- TestPerGroupConfig_CacheInvalidation (Phase 1)
- TestPerGroupConfig_ClaudeConfigDirSourceLabel (Phase 2 / plan 02-02)
- TestPerGroupConfig_ClaudeConfigResolutionLogFormat (Phase 2 / plan 02-02)

Path A taken; Paths B (synthesize fresh) and C (defer) not needed — `git show 5013940^:CLAUDE.md` returned the expected 460-line v1.5.2 content with the `Session persistence: mandatory test coverage` heading intact.

Base implementation by @alec-pinson in PR #578.

Refs: CFG-06, REQ-6
Committed by Ashesh Goplani
EOF
)"
```

If Path B was taken (Step 1 returned no match), replace the first paragraph of the commit body with a Path B justification and omit the "Path A taken; Paths B and C not needed" line. If `git add -f` was required, add a line "Staged with `git add -f` (path-ignored in a higher-precedence ignore source)." to the body.

Hard rules: NO `--no-verify`, NO push/tag/PR/merge, NO Claude attribution.
  </action>
  <verify>
    <automated>[ -f CLAUDE.md ] &amp;&amp; grep -c "TestPerGroupConfig_" CLAUDE.md | grep -q "^1$" &amp;&amp; grep -q "Per-group config dir applies to custom-command sessions too" CLAUDE.md &amp;&amp; grep -q "^## Session persistence" CLAUDE.md &amp;&amp; git ls-files --error-unmatch CLAUDE.md &amp;&amp; git log -1 --pretty=%s HEAD | grep -q "^docs(claude): resurrect repo-root CLAUDE.md and add TestPerGroupConfig_ enforcement one-liner (CFG-06)$" &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Base implementation by @alec-pinson in PR #578." &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Committed by Ashesh Goplani" &amp;&amp; ! git log -1 --pretty=%B HEAD | grep -qE "Generated with Claude|Co-Authored-By: Claude"</automated>
  </verify>
  <acceptance_criteria>
    - File `CLAUDE.md` exists in the repo root AND is tracked (`git ls-files --error-unmatch CLAUDE.md` returns 0).
    - `grep -c "TestPerGroupConfig_" CLAUDE.md` returns exactly `1`.
    - CLAUDE.md contains the exact substring `Per-group config dir applies to custom-command sessions too;`.
    - CLAUDE.md contains the `## Session persistence: mandatory test coverage` heading (Path A) OR a freshly-synthesized equivalent session-persistence heading (Path B — documented in commit body).
    - Commit subject exactly equals `docs(claude): resurrect repo-root CLAUDE.md and add TestPerGroupConfig_ enforcement one-liner (CFG-06)`.
    - Commit body contains `Base implementation by @alec-pinson in PR #578.` AND `Committed by Ashesh Goplani`.
    - Commit body does NOT contain `Generated with Claude` or `Co-Authored-By: Claude`.
    - `git diff-tree --no-commit-id --name-only -r HEAD` lists exactly `CLAUDE.md`.
  </acceptance_criteria>
  <done>CLAUDE.md resurrected, one-liner inserted, file tracked, commit landed with attribution, all acceptance greps pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG [Unreleased] > Added bullet for per-group Claude config overrides</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-12 — confirms `## [Unreleased]` is at line 8 and currently empty; Keep-a-Changelog format)
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-CONTEXT.md (§CFG-06 CHANGELOG bullet + §specifics re: `## [Unreleased]` block currently empty; `### Added` must be a sibling sub-heading)
    - .planning/REQUIREMENTS.md (CFG-06 bullet text at L52)
  </read_first>
  <action>
Edit `CHANGELOG.md`. The current state has `## [Unreleased]` at line 8 with an empty body (line 9 is blank, line 10 begins `## [1.5.1]`). Insert the following between line 8 and line 10, preserving the blank-line separator pattern used elsewhere in the file:

```markdown

### Added
- Per-group Claude config overrides (`[groups."<name>".claude]`). (Base implementation by @alec-pinson in [PR #578](https://github.com/asheshgoplani/agent-deck/pull/578))

```

After the edit, line 8 is still `## [Unreleased]`. Line 9 is blank. Line 10 is `### Added`. Line 11 is the bullet. Line 12 is blank. Line 13 is `## [1.5.1] - 2026-04-13`.

Verification-friendly edit: run `awk '/^## \[Unreleased\]/{print; getline; print; print "### Added"; print "- Per-group Claude config overrides (`[groups.\"<name>\".claude]`). (Base implementation by @alec-pinson in [PR #578](https://github.com/asheshgoplani/agent-deck/pull/578))"; print ""; next}1' CHANGELOG.md &gt; /tmp/changelog.new &amp;&amp; mv /tmp/changelog.new CHANGELOG.md` — OR hand-edit with the Edit tool. Either is fine; idempotency must hold (running the edit twice must not duplicate the bullet).

Post-edit sanity:
```bash
sed -n '8,14p' CHANGELOG.md
```
Expected output:
```
## [Unreleased]

### Added
- Per-group Claude config overrides (`[groups."<name>".claude]`). (Base implementation by @alec-pinson in [PR #578](https://github.com/asheshgoplani/agent-deck/pull/578))

## [1.5.1] - 2026-04-13
```

Stage and commit:

```bash
git add CHANGELOG.md
git commit -m "$(cat &lt;&lt;'EOF'
docs(changelog): add [Unreleased] Added bullet for per-group Claude config overrides (CFG-06)

Populates the previously-empty `## [Unreleased]` block (at line 8) with
an `### Added` subsection and one bullet for per-group Claude config
overrides. The bullet references PR #578 as the base implementation per
Keep a Changelog conventions and the milestone attribution rule.

New line in CHANGELOG.md:
- Per-group Claude config overrides (`[groups."<name>".claude]`). (Base implementation by @alec-pinson in [PR #578](https://github.com/asheshgoplani/agent-deck/pull/578))

No other sections of CHANGELOG.md were touched.

Base implementation by @alec-pinson in PR #578.

Refs: CFG-06, REQ-6
Committed by Ashesh Goplani
EOF
)"
```

Hard rules: NO `--no-verify`, NO push/tag/PR/merge, NO Claude attribution.
  </action>
  <verify>
    <automated>grep -c "^### Added$" CHANGELOG.md | awk '{exit !($1&gt;=1)}' &amp;&amp; grep -q 'Per-group Claude config overrides' CHANGELOG.md &amp;&amp; grep -q '\[groups."&lt;name&gt;".claude\]' CHANGELOG.md &amp;&amp; grep -q 'github.com/asheshgoplani/agent-deck/pull/578' CHANGELOG.md &amp;&amp; awk '/^## \[Unreleased\]/{f=1; next} /^## \[/{f=0} f &amp;&amp; /^- Per-group Claude config overrides/' CHANGELOG.md | grep -q 'Per-group Claude config overrides' &amp;&amp; git log -1 --pretty=%s HEAD | grep -q "^docs(changelog): add \[Unreleased\] Added bullet for per-group Claude config overrides (CFG-06)$" &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Base implementation by @alec-pinson in PR #578." &amp;&amp; git log -1 --pretty=%b HEAD | grep -q "Committed by Ashesh Goplani" &amp;&amp; ! git log -1 --pretty=%B HEAD | grep -qE "Generated with Claude|Co-Authored-By: Claude"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `### Added` as a sibling heading under `## [Unreleased]` (awk window assertion — bullet appears between `## [Unreleased]` and the next `## [` heading).
    - CHANGELOG.md contains the literal bullet `- Per-group Claude config overrides (\`[groups."<name>".claude]\`).` with the PR #578 reference appended.
    - CHANGELOG.md contains `github.com/asheshgoplani/agent-deck/pull/578`.
    - The `## [1.5.1] - 2026-04-13` heading and all subsequent content is unchanged (no renumbering).
    - Commit subject exactly equals `docs(changelog): add [Unreleased] Added bullet for per-group Claude config overrides (CFG-06)`.
    - Commit body contains `Base implementation by @alec-pinson in PR #578.` AND `Committed by Ashesh Goplani`.
    - Commit body does NOT contain `Generated with Claude` or `Co-Authored-By: Claude`.
    - `git diff-tree --no-commit-id --name-only -r HEAD` lists exactly `CHANGELOG.md`.
  </acceptance_criteria>
  <done>CHANGELOG bullet inserted at the correct seam, commit landed with attribution, all acceptance greps pass.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-wide hard-rule audit — attribution count, no Claude attribution, no push/tag/PR/merge</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (§Hard rules in force — all 7 rules must hold on `main..HEAD`)
    - .planning/ROADMAP.md (§Milestone Verification block, criteria 5 and 6 — attribution + no-push audit)
    - .planning/phases/03-visual-harness-documentation-attribution-commit/03-CONTEXT.md (§CFG-06 attribution commit verification: `git log main..HEAD --grep "@alec-pinson"` returns ≥ 1 commit)
  </read_first>
  <action>
This is a pure audit task — no files are modified, no commit is created. Run the following commands IN ORDER and capture output to a phase artifact log (uncommitted). If ANY audit check fails, execution HALTS and the failing tasks must be remediated before advancing to `/gsd-verify-phase`.

```bash
mkdir -p .planning/phases/03-visual-harness-documentation-attribution-commit/artifacts
AUDIT_LOG=".planning/phases/03-visual-harness-documentation-attribution-commit/artifacts/hard-rule-audit-$(date -u +%Y%m%dT%H%M%SZ).log"
{
    echo "=== Hard-rule audit for Phase 3 (main..HEAD) ==="
    echo "Timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
    echo ""

    echo "--- 1. @alec-pinson attribution count (must be ≥ 2) ---"
    git log main..HEAD --grep "@alec-pinson" --oneline
    ATTRIB_COUNT=$(git log main..HEAD --grep "@alec-pinson" --oneline | wc -l)
    echo "Count: $ATTRIB_COUNT"
    if [ "$ATTRIB_COUNT" -lt 2 ]; then echo "FAIL: attribution count &lt; 2"; exit 10; fi
    echo "PASS"
    echo ""

    echo "--- 2. No Claude attribution on main..HEAD (must be 0) ---"
    git log main..HEAD --grep "Generated with Claude\|Co-Authored-By: Claude" --oneline
    CLAUDE_COUNT=$(git log main..HEAD --grep "Generated with Claude\|Co-Authored-By: Claude" --oneline | wc -l)
    echo "Count: $CLAUDE_COUNT"
    if [ "$CLAUDE_COUNT" -gt 0 ]; then echo "FAIL: Claude attribution found"; exit 11; fi
    echo "PASS"
    echo ""

    echo "--- 3. Every commit on main..HEAD carries 'Committed by Ashesh Goplani' trailer ---"
    MISSING_SIG=0
    for sha in $(git rev-list main..HEAD); do
        if ! git log -1 --pretty=%B "$sha" | grep -q "Committed by Ashesh Goplani"; then
            echo "MISSING signature on $sha: $(git log -1 --pretty=%s "$sha")"
            MISSING_SIG=$((MISSING_SIG+1))
        fi
    done
    if [ "$MISSING_SIG" -gt 0 ]; then echo "FAIL: $MISSING_SIG commit(s) missing signature"; exit 12; fi
    echo "PASS: all commits signed"
    echo ""

    echo "--- 4. No git push / tag / gh pr-create / gh pr-merge / gh release in reflog for this phase ---"
    # Reflog scan: look at the working-copy reflog and flag any operation that invoked the forbidden verbs.
    REFLOG_HITS=$(git reflog --date=iso | grep -iE "push|\btag\b|pr create|pr merge|gh release" | wc -l)
    echo "Reflog hits for forbidden ops: $REFLOG_HITS"
    # Reflog is advisory — the definitive check is that the remote has no diff from base.
    echo "Branch state (sanity):"
    git status --short
    git log -1 --pretty='%H %s' HEAD
    echo ""

    echo "--- 5. File-level sanity: Phase 3 touched exactly the expected files ---"
    CHANGED_FILES=$(git diff --name-only main..HEAD | sort -u)
    echo "Changed files on main..HEAD:"
    echo "$CHANGED_FILES"
    # Expected set (Phase 3 scope + carry-forward from Phase 1/2):
    # Phase 3: scripts/verify-per-group-claude-config.sh, README.md, CLAUDE.md, CHANGELOG.md
    # Phase 1/2 already-touched files are expected to appear too (pergroupconfig_test.go, instance.go, claude.go, etc.) — they are not Phase 3 regressions.
    # This check only FAILS if a file outside the CONTEXT.md scope list appears. The executor reviews the list manually.
    echo ""

    echo "--- 6. Summary ---"
    echo "Attribution commits: $ATTRIB_COUNT"
    echo "Claude attribution commits: $CLAUDE_COUNT"
    echo "Unsigned commits: $MISSING_SIG"
    echo "Audit: PASS"
} | tee "$AUDIT_LOG"
```

After the audit log prints, the executor reviews the changed-files list (check 5) for any out-of-scope path (anything outside the list in CONTEXT.md `<domain>` in-scope block). If an out-of-scope file appears, the executor returns to the offending task and fixes it (revert the file, recommit with narrow scope). Do NOT create new commits in this audit task.

This task creates NO git commits. The audit log is for the 03-02-SUMMARY.md and is NOT committed.
  </action>
  <verify>
    <automated>ATTRIB=$(git log main..HEAD --grep "@alec-pinson" --oneline | wc -l); CLAUDE=$(git log main..HEAD --grep "Generated with Claude\|Co-Authored-By: Claude" --oneline | wc -l); MISSING=0; for sha in $(git rev-list main..HEAD); do if ! git log -1 --pretty=%B "$sha" | grep -q "Committed by Ashesh Goplani"; then MISSING=$((MISSING+1)); fi; done; [ "$ATTRIB" -ge 2 ] &amp;&amp; [ "$CLAUDE" -eq 0 ] &amp;&amp; [ "$MISSING" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log main..HEAD --grep "@alec-pinson" --oneline | wc -l` returns ≥ 2.
    - `git log main..HEAD --grep "Generated with Claude\|Co-Authored-By: Claude" --oneline | wc -l` returns exactly 0.
    - Every commit on `main..HEAD` (scanned via `git rev-list main..HEAD`) has `Committed by Ashesh Goplani` in its full message body.
    - `git diff --name-only main..HEAD` shows only files from the CONTEXT.md scope list (Phase 1+2 + Phase 3 files; no drift).
    - Audit log written to `.planning/phases/03-visual-harness-documentation-attribution-commit/artifacts/hard-rule-audit-*.log` (uncommitted).
    - No new commits created by this task.
  </acceptance_criteria>
  <done>Audit log produced, all four gates PASS, no file changes, no new commits. Phase is ready for `/gsd-verify-phase`.</done>
</task>

</tasks>

<verification>
Plan 02 combined-level gates:
- `grep -c "^### Per-group Claude config$" README.md` returns exactly 1.
- `grep -c "TestPerGroupConfig_" CLAUDE.md` returns exactly 1; `git ls-files --error-unmatch CLAUDE.md` succeeds.
- `grep -c "Per-group Claude config overrides" CHANGELOG.md` returns ≥ 1; the bullet is between `## [Unreleased]` and `## [1.5.1]` in the file.
- `git log main..HEAD --grep "@alec-pinson" --oneline | wc -l` returns ≥ 2 (Plan 01 harness commit + at least the README or CHANGELOG commit from this plan).
- `git log main..HEAD --grep "Generated with Claude\|Co-Authored-By: Claude" --oneline | wc -l` returns 0.
- Every commit on `main..HEAD` has `Committed by Ashesh Goplani` in its body.
- No out-of-scope files on `main..HEAD` beyond what Phase 1, 2, and 3 specify.
- No `git push`, `git tag`, `gh release`, `gh pr create`, `gh pr merge` was invoked at any point during this plan.
</verification>

<success_criteria>
1. README.md has the new `### Per-group Claude config` subsection with the locked TOML block, harness pointer, and priority summary — inserted between `### Skills Manager` and `### MCP Socket Pool`.
2. CLAUDE.md exists at repo root (Path A from `5013940^`), is tracked, and contains the exact one-liner `Per-group config dir applies to custom-command sessions too; \`TestPerGroupConfig_*\` suite enforces this.` under a `## Session persistence` heading.
3. CHANGELOG.md `[Unreleased]` block has `### Added` with the locked bullet (including the PR #578 reference).
4. Three commits on `main..HEAD` from this plan: `docs(readme): ...`, `docs(claude): ...`, `docs(changelog): ...`. Each body carries `Base implementation by @alec-pinson in PR #578.` and `Committed by Ashesh Goplani`.
5. `git log main..HEAD --grep "@alec-pinson"` returns ≥ 2 commits (including Plan 01's harness commit).
6. No Claude attribution on any commit in `main..HEAD`. No `git push`, `git tag`, `gh release`, `gh pr create`, `gh pr merge` executed anywhere in Phase 3.
7. Hard-rule audit log captured to `.planning/phases/03-visual-harness-documentation-attribution-commit/artifacts/hard-rule-audit-*.log` (uncommitted).
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-harness-documentation-attribution-commit/03-02-SUMMARY.md` following `.claude/get-shit-done/templates/summary.md`. Include:
- For each doc task (README, CLAUDE.md, CHANGELOG): commit hash + subject + acceptance grep outputs.
- For the CLAUDE.md task: explicit record of which Path (A/B/C) was taken and why.
- Task 4 audit log contents (copy the PASS/FAIL block from the tee'd log into the summary).
- Final `git log main..HEAD --oneline` output for the phase — confirms commit order and count.
- Any deviations from the plan (e.g., Path B fallback, unexpected lefthook rejection and how it was resolved).
</output>
